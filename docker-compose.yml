services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_DB=autopulse
      - POSTGRES_USER=paul
      - POSTGRES_PASSWORD=autopulse
    ports:
      - "5432:5432"
    volumes:
      - autopulse_pgdata:/var/lib/postgresql/data

volumes:
  autopulse_pgdata:
